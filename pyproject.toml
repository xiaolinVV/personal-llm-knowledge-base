[project]
name = "ai-agent-fieldbook"
version = "0.1.0"
description = "Fieldbook for AI Agent learning research, case studies, and focused experiments."
requires-python = ">=3.11"
dependencies = [
    "openai",
    "openai-agents>=0.17.0",
]

[tool.ruff]
line-length = 100
