[project]
name = "personal-llm-knowledge-base"
version = "0.1.0"
description = "Personal LLM knowledge base for notes, research, experiments, and outputs."
requires-python = ">=3.11"
dependencies = [
    "openai",
    "openai-agents>=0.17.0",
]

[tool.ruff]
line-length = 100
